# Runtime dependencies for the embed / query / vector-search skill scripts.
# Authentication uses Application Default Credentials via google-auth — the
# scripts never invoke `gcloud ... print-access-token` or handle a raw token.
google-auth>=2.0.0
requests>=2.0.0

# Docs site (mkdocs) — not required to run the skills.
mkdocs>=1.5.0
mkdocs-material>=9.5.0
pymdown-extensions>=10.7
